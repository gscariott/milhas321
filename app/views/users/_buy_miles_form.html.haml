= form_with(url: buy_miles_user_path(current_user), local: true) do |form|
  .mb-3
    = form.label 'Quantidade', class: 'form-label'
    = form.number_field :quantity, min: 1, class: 'form-control'
  = form.submit 'Enviar', class: 'btn btn-dark'

= form_with(url: update_site_path(site), method: :patch, local: true) do |form|
  .field
    = form.label 'Novo valor da milha'
    = form.number_field :mile_price, min: 0.01, step: 0.01
  .actions
    = form.submit 'Enviar'
%h1 Passagens Aéreas

%table
  %thead
    %tr
      %th Companhia Aérea
      %th Voo
      %th Aeronave
      %th Lote
      %th Partida
      %th Data máxima para cancelamento
      %th Origem
      %th Destino
      %th Preço
      %th
      %th
      %th

  %tbody
    - @tickets.each do |ticket|
      %tr
        %td= ticket.airline.name
        %td= ticket.flight
        %td= ticket.airplane
        %td= ticket.batch
        %td= ticket.departure
        %td= ticket.max_cancellation_date
        %td= ticket.from
        %td= ticket.to
        %td= ticket.value
        %td= link_to 'Detalhes', ticket
        %td= link_to 'Editar', edit_ticket_path(ticket)
        %td= link_to 'Deletar', ticket, method: :delete, data: { confirm: 'Tem certeza?' }

%br
